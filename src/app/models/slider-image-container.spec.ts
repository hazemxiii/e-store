import { SliderImageContainer } from './slider-image-container';

describe('SliderImageContainer', () => {
  it('should create an instance', () => {
    expect(new SliderImageContainer()).toBeTruthy();
  });
});
