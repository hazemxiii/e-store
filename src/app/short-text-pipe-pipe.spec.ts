import { ShortTextPipePipe } from './short-text-pipe-pipe';

describe('ShortTextPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ShortTextPipePipe();
    expect(pipe).toBeTruthy();
  });
});
